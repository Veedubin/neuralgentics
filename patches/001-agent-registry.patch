diff --git a/packages/opencode/src/agent/agent.ts b/packages/opencode/src/agent/agent.ts
index e78304e4e..3babe33a0 100644
--- a/packages/opencode/src/agent/agent.ts
+++ b/packages/opencode/src/agent/agent.ts
@@ -278,6 +278,96 @@ export const layer = Layer.effect(
             ),
             prompt: PROMPT_SUMMARY,
           },
+          neuralgentics: {
+            name: "neuralgentics",
+            description: "Neuralgentics Orchestrator — routes tasks to specialist agents",
+            mode: "all" as const,
+            model: Provider.parseModel("ollama-cloud/kimi-k2.6:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_architect: {
+            name: "neuralgentics_architect",
+            description: "Neuralgentics Architect — system design + trade-off analysis",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/deepseek-v4-pro:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_coder: {
+            name: "neuralgentics_coder",
+            description: "Neuralgentics Coder — fast TypeScript/Python implementation",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/glm-5.1:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_explorer: {
+            name: "neuralgentics_explorer",
+            description: "Neuralgentics Explorer — file finding only",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/devstral-2:123b-cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_tester: {
+            name: "neuralgentics_tester",
+            description: "Neuralgentics Tester — test writing and execution",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/deepseek-v4-flash:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_linter: {
+            name: "neuralgentics_linter",
+            description: "Neuralgentics Linter — code style enforcement",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/qwen3-coder-next:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_git: {
+            name: "neuralgentics_git",
+            description: "Neuralgentics Git — version control operations",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/minimax-m2.7:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_writer: {
+            name: "neuralgentics_writer",
+            description: "Neuralgentics Writer — documentation and markdown",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/gemma4:31b-cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
+          neuralgentics_reviewer: {
+            name: "neuralgentics_reviewer",
+            description: "Neuralgentics Reviewer — code review and security audit",
+            mode: "subagent" as const,
+            model: Provider.parseModel("ollama-cloud/qwen3.5:cloud"),
+            steps: 50,
+            permission: Permission.merge(defaults, user),
+            options: {},
+            native: false,
+          },
         }
 
         for (const [key, value] of Object.entries(cfg.agent ?? {})) {
